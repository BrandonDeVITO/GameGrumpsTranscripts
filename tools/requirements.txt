# No external dependencies required.
# gg.py uses only Python standard library modules:
#   argparse, json, re, sqlite3, sys, pathlib
#
# Requires: Python 3.8+ with SQLite compiled with FTS5 support
# (CPython ships FTS5 by default since 3.8)
#
# Usage:
#   python tools/gg.py build-index       <- one-time setup
#   python tools/gg.py "banana"          <- search
#   python tools/gg.py "banana" "cake"   <- co-occurrence
#   python tools/gg.py --stats "banana"  <- stats
